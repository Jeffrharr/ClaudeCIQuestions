# Local, private interview transcripts/notes — never commit
transcripts/
*.transcript.md
# NOTE: .understanding/ is the vendored gate dir (set-status.sh) and IS tracked —
# do not ignore it. Keep private notes under transcripts/ instead.

# Secrets / env
.env
.env.*

# Node
node_modules/

# Python
__pycache__/
*.pyc
.venv/

# OS / editor
.DS_Store
*.swp
